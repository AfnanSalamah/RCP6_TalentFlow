// Deprecated mock data module. Kept only so older components compile while all
// live records are loaded through FastAPI + SQLite APIs.
export const projects = [];
export const roles = [];
export const candidates = [];
export const interviews = [];
export const talentPoolTags = [];
export const dashboardStats = {};
